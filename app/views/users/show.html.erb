<% provide(:title, @user.name) %>
<div class="row">
  <aside class="container-fluid">
    <section>
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
      <%= render 'shared/admin' %>
    </section>
  </aside>
</div>
