<a href="<%= user_path(current_user) %>">
  <%= gravatar_for current_user, size: 110 %>
</a>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "Profile", current_user %>
</span>
<span>
  <%= pluralize(current_user.soundposts.count, "soundpost") %>
</span>
